# SKSE Menu Framework Header

SEDAS can build its in-game configuration pages when this directory contains:

```text
SKSEMenuFramework.h
```

Fetch the v3 header from the framework source:

```powershell
.\scripts\fetch-skmf-header.ps1
```

If the header is absent, the DLL still builds with file-based TOML configuration.
